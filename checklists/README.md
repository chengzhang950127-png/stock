# Acceptance checklists

Each Work Package landing in this repo should drop a checklist file here
named `WP-<id>.md`. The checklist mirrors the WP's "Definition of Done" and
gets ticked off during three-party review (see `docs/review-protocol.md`).

Phase 0 owns no WP — checklists begin arriving with WP-1.x.
